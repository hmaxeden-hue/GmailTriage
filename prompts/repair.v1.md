Deine letzte Antwort entsprach nicht dem geforderten Schema.

Fehler:
{{FEHLER}}

Deine Antwort war:
{{ANTWORT}}

Gib dieselbe Einordnung erneut aus, diesmal exakt im geforderten Schema.
Antworte ausschliesslich mit dem JSON-Objekt, ohne Codeblock und ohne
Begleittext. Erfinde keine Werte, um Felder zu füllen — was du nicht weisst,
bleibt `null`.
